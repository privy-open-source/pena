{
  "name": "@privyid/pena-monorepo",
  "packageManager": "yarn@3.3.1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "yarn workspaces foreach -vtp run build",
    "check": "yarn dlx @yarnpkg/doctor packages/",
    "coverage": "yarn workspaces foreach -vtp run coverage"
  },
  "workspaces": [
    "packages/*"
  ]
}
